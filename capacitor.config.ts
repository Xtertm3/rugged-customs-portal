import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ruggedcustoms.portal',
  appName: 'Rugged Customs Portal',
  webDir: 'dist'
};

export default config;
